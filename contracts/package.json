{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Web3 Dating App",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:syscoin": "hardhat run scripts/deploy.js --network syscoinTestnet",
    "deploy:rollux": "hardhat run scripts/deploy.js --network rolluxTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network syscoinMainnet",
    "seed:syscoin": "hardhat run scripts/seed.js --network syscoinTestnet",
    "seed:rollux": "hardhat run scripts/seed.js --network rolluxTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1"
  }
}