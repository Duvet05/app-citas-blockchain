# Dockerfile para Hardhat Node
FROM node:18-alpine

WORKDIR /app/contracts

# Copiar package.json
COPY contracts/package.json ./

# Instalar dependencias
RUN npm ci

# Copiar el resto de archivos
COPY contracts/ ./

# Exponer puerto de Hardhat
EXPOSE 8545

# Comando para iniciar Hardhat node
CMD ["npx", "hardhat", "node", "--hostname", "0.0.0.0"]
